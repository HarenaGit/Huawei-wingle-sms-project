import Login from "../../connection/Login";
export declare class Ussd {
    private login;
    private connection;
    private logger;
    constructor(login: Login);
    activeLog(activeLog: boolean): void;
    sendUssd(ussd: string): Promise<string>;
    reply(response: string): Promise<string>;
    private retrieveResponseFromRemote;
    private sleep;
}
//# sourceMappingURL=Ussd.d.ts.map