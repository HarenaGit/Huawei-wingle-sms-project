import Connection from "../../connection/Connection";
import Network from "../../model/home/Network";
import { NetworkStatus } from '../../model/home/NetworkStatus';
export default class NetworktExtractor {
    private connection;
    private logger;
    constructor(connection: Connection);
    activeLog(activeLog: boolean): void;
    getNetwork(): Promise<Network>;
    private getOperator;
    private connectStatus;
    private getType;
    private getSignal;
    static getNetworkStatus(document: Document): NetworkStatus;
}
//# sourceMappingURL=NetworkExtractor.d.ts.map