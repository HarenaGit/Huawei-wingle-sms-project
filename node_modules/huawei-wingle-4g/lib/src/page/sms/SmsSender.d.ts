import Login from "../../connection/Login";
export default class {
    private login;
    private connection;
    private logger;
    constructor(login: Login);
    activeLog(activeLog: boolean): void;
    sendSms(phoneNumbers: string | string[], content: string): Promise<Map<string, boolean>>;
    private buildParameters;
    private getDate;
    private verifySmsSent;
    private sleep;
    private getStatusDocument;
    private allDone;
    private getPhoneNumbersInDocument;
}
//# sourceMappingURL=SmsSender.d.ts.map