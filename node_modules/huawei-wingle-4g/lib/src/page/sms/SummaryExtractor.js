"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const log4js_1 = __importDefault(require("log4js"));
const StringUtils_1 = require("../../utils/StringUtils");
const ElemenUtils_1 = __importDefault(require("../../utils/ElemenUtils"));
class default_1 {
    constructor(login) {
        this.login = login;
        this.connection = login.getConnnection();
        this.logger = log4js_1.default.getLogger(StringUtils_1.substringAfter(__filename, 'huawei-wingle-4g'));
    }
    activeLog(activeLog) {
        this.logger.level = activeLog ? 'debug' : 'OFF';
        this.login.activeLog(activeLog);
    }
    getSummary() {
        return __awaiter(this, void 0, void 0, function* () {
            yield this.connection.openHomePage();
            yield this.login.login();
            const response = yield this.connection.get('/api/sms/sms-count');
            const document = response.document;
            const unread = ElemenUtils_1.default(document, 'LocalUnread', 'Unread received SMS', this.logger);
            const total = ElemenUtils_1.default(document, 'LocalInbox', 'Total received SMS', this.logger);
            const sent = ElemenUtils_1.default(document, 'LocalOutbox', 'Sent SMS', this.logger);
            const draft = ElemenUtils_1.default(document, 'LocalDraft', 'Draft SMS', this.logger);
            return {
                inbox: {
                    unread: +unread,
                    total: +total
                },
                outbox: +sent,
                draft: +draft
            };
        });
    }
}
exports.default = default_1;
//# sourceMappingURL=SummaryExtractor.js.map