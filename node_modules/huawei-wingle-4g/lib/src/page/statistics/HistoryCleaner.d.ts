import Login from "../../connection/Login";
export default class {
    private login;
    private connection;
    private logger;
    constructor(login: Login);
    activeLog(activeLog: boolean): void;
    clearHistory(): Promise<void>;
}
//# sourceMappingURL=HistoryCleaner.d.ts.map