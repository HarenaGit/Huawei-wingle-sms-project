import Login from "../../connection/Login";
import WlanClient from "../../model/statistics/WlanClient";
export default class {
    private login;
    private connection;
    private logger;
    constructor(login: Login);
    activeLog(activeLog: boolean): void;
    getWlanClients(): Promise<WlanClient[]>;
    private getDuration;
}
//# sourceMappingURL=WlanClientsExtractor.d.ts.map