import Login from "../../connection/Login";
import DataPlan from "../../model/statistics/DataPlan";
export default class {
    private login;
    private connection;
    private logger;
    constructor(login: Login);
    activeLog(activeLog: boolean): void;
    updateDataPlan(dataPlan: DataPlan): Promise<void>;
    private buildParameters;
    private getStartDate;
    private getDataLimit;
    private getThreshold;
    private isPositiveInteger;
}
//# sourceMappingURL=DataPlanning.d.ts.map